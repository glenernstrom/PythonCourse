# Functions
	- ## Recycling fucntions with
		- if __name__ == '__main___':
		- What does this mean? What does "name" refer to? What is "main"? Whatever they mean, anything after this (with proper indentation) is code used at runtime. Anything above it, like functions, can be imported into other code.
- # Loops
- [[while loops]]: interesting, you can set a bool literal 'True' as the [[while condition]] because it is [[truthy]], then break the loop when desired result is obtained; e.g., when asking for the user to input a correct input, the loop will continue infinitely until the correct input is obtained.
-